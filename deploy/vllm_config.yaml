# Leave GPU headroom for the host; the layout pipeline runs in Triton on CPU.
gpu-memory-utilization: 0.40
max-model-len: 8192
max-num-seqs: 2
max-num-batched-tokens: 8192
enforce-eager: true
mm-processor-cache-gb: 0
